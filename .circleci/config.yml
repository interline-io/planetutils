version: 2
jobs:
  build:
    docker:
      - image: circleci/python:2.7
    steps:
      - checkout
      - run: pip install --user .
      - run:
        name: Running tests
        command: tox
        environment:
          PATH: "$PATH;~/.local/bin"
